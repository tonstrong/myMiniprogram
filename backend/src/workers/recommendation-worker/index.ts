import type { AppContext } from "../../app/bootstrap/app";

export async function runRecommendationWorker(context: AppContext) {
  context.logger.info("Recommendation worker started (placeholder)");
}
